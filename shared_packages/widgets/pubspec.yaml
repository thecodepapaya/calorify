name: widgets
description: Shared widgets for Calorify app and watch companion
version: 1.2.1
publish_to: 'none'

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^1.0.0
  i18n:
    path: ../i18n
  lucide_icons_flutter: ^3.1.1
  intl: ^0.20.2
  models:
    path: ../models
  specs:
    path: ../specs

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
