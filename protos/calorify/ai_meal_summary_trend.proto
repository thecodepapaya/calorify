syntax = "proto3";

package calorify.ai_meal_summary;

// Calorie trend vs prior window for GET /api/v1/food/ai-summary (proto3 JSON: UNSPECIFIED, UP, DOWN, STEADY).
enum AiMealSummaryTrend {
  UNSPECIFIED = 0;
  UP = 1;
  DOWN = 2;
  STEADY = 3;
}
