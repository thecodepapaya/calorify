auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096

common:
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2020-10-24
      store: boltdb-shipper
      object_store: filesystem
      schema: v11
      index:
        prefix: index_
        period: 24h

ruler:
  alertmanager_url: http://localhost:9093

# Loki will retain logs for 672 hours (4 weeks / 28 days)
limits_config:
  retention_period: 672h
  ingestion_rate_mb: 16
  ingestion_burst_size_mb: 32
  max_query_length: 720h  # Allow querying up to 30 days
  max_query_parallelism: 32
  reject_old_samples: true
  reject_old_samples_max_age: 672h  # Allow logs up to 4 weeks old
  creation_grace_period: 10m  # Grace period for clock skew

compactor:
  working_directory: /loki/compactor
  shared_store: filesystem
  compaction_interval: 10m
  retention_enabled: true
  retention_delete_delay: 2h
  retention_delete_worker_count: 150

table_manager:
  retention_deletes_enabled: true
  retention_period: 672h
